using UnityEngine;
using System.Collections;

public class PaintSpotScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

    void Kill()
    {
        Destroy(this);
    }
}
